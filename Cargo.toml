[workspace]
resolver = "2"
members = [
    "crates/coppermoon",
    "crates/coppermoon_core",
    "crates/coppermoon_std",
    "crates/harbor",
    "crates/shipyard",
    "crates/quarry",
    "crates/andesite",
    "crates/sqlite",
    "crates/mysql",
    "crates/postgresql",
]

[workspace.package]
version = "0.1.61"
edition = "2021"
authors = ["CopperMoon Contributors"]
license = "MIT"
repository = "https://github.com/coppermoon/coppermoon"
description = "A high-performance Lua runtime written in Rust"

# Optimized release binaries (andesite ships itself as the app runtime).
# NOTE: no `panic = "abort"` — mlua requires unwinding across the Lua boundary.
[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true

[workspace.dependencies]
# Lua integration
mlua = { version = "0.10", features = ["lua54", "vendored", "async", "send", "serialize"] }

# Async runtime
tokio = { version = "1.43", features = ["full"] }

# CLI
clap = { version = "4.5", features = ["derive"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Error handling
thiserror = "2.0"
anyhow = "1.0"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Utils
colored = "2.2"
libloading = "0.8"
